<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #f5deb3, #fdf5e6);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    #Profile_Card {
      background-color: bisque;
      padding: 25px 35px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
      width: 400px;
      text-align: center;
    }
    #Profile_Card h2 {
      margin-bottom: 20px;
      color: #444;
    }
    .profile-info {
      text-align: left;
      margin-top: 15px;
    }
    .profile-info p {
      margin: 8px 0;
    }
    button {
      margin-top: 15px;
      padding: 10px 18px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      background: #f4a261;
      color: #fff;
    }
    button:hover { background: #e07a5f; }
  </style>
</head>
<body>
  <div id="Profile_Card">
    <h2>Welcome, <%= user.username %> 🎉</h2>
    <div class="profile-info">
      <p><strong>Email:</strong> <%= user.email %></p>
      <p><strong>Created At:</strong> <%= user.creation_date %></p>
      <p><strong>Status:</strong> <%= user.logged_in_status ? "Online" : "Offline" %></p>
    </div>
    <button onclick="window.location='/'">Back to Home</button>
  </div>
</body>
</html>
