<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #f5deb3, #fdf5e6);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    #SignUp_Card {
      background-color: bisque;
      padding: 25px 35px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
      width: 350px;
      text-align: center;
    }
    #SignUp_Card h2 { margin-bottom: 20px; color: #444; }
    table { width: 100%; }
    table td { padding: 10px; }
    input[type="text"], input[type="password"] {
      width: 100%; padding: 10px;
      border: 1px solid #bbb; border-radius: 6px;
    }
    button {
      padding: 10px 18px; border: none; border-radius: 6px;
      cursor: pointer; font-size: 14px;
    }
    button[type="submit"] { background: #f4a261; color: #fff; }
    button[type="submit"]:hover { background: #e07a5f; }
    button[type="button"] { background: #ccc; }
    button[type="button"]:hover { background: #999; }
  </style>
</head>
<body>
  <div id="SignUp_Card">
    <h2>Sign Up</h2>
    <form action="/register" method="POST">
      <table>
        <tr>
          <td><label for="username">Username:</label></td>
          <td><input type="text" name="username" placeholder="Username" required></td>
        </tr>
        <tr>
          <td><label for="email">Email:</label></td>
          <td><input type="text" name="email" placeholder="Email Address" required></td>
        </tr>
        <tr>
          <td><label for="password">Password:</label></td>
          <td><input type="password" name="password" placeholder="New Password" required></td>
        </tr>
        <tr>
          <td><button type="submit">Sign Up</button></td>
          <td><button type="button" onclick="window.location='/'">Cancel</button></td>
        </tr>
      </table>
    </form>
  </div>
</body>
</html>
